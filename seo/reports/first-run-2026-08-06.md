# First-Run Summary — casheshair.com — 2026-08-06

Ran: keyword research → technical audit → analytics → GEO. Full detail in
the sibling reports. Nothing was published; this cycle is analysis + queues.

## Top 5 things to do (highest impact)
1. **Noindex 42 thin archive pages** (Yoast; owner, ~15 min). Confirmed
   indexable and competing with real pages.
2. **Assign products to the empty method categories** (tape-in / i-tip /
   k-tip / hybrid-weft) in wp-admin — they're commercial landing pages
   ranking for nothing because they contain 0 products.
3. **Fix the category pages** (raw bundles, methods): add missing meta
   descriptions + 200–400 words of buying guidance + FAQ. → On-Page agent.
4. **Add LocalBusiness schema + Chicago page** for local + AI visibility.
   → Local agent.
5. **Export Search Console data** to `seo/data/gsc/` so the striking-distance
   sprint (fastest traffic win) can run. → owner.

## Waiting on owner
- wp-admin: noindex archives; assign method products to categories; fix
  homepage double-H1.
- Export GSC Performance CSV (last 3 months, Query + Page) → `seo/data/gsc/`.
- Run the GEO test panel once to set the AI-citation baseline (geo report).
- Fill voice TODOs (stories/stats/opinions) so the Blog agent can draft in
  your real voice.

## Queues created
- `seo/data/keywords.csv` — commercial targets mapped to pages; 7 blog
  topics queued; empty-category blockers flagged.
- On-Page queue: category metas/copy/FAQ, sew-in post meta + H1 de-dupe.
- Blog queue: tape-in vs i-tip vs k-tip (first), 4c texture-match (second).
- Local queue: LocalBusiness schema, Chicago page, GBP checklist.

## Suggested next step
`/seo-team weekly` will draft the top On-Page refreshes + the first 2 blog
posts (as WordPress drafts for your approval). Most effective after the GSC
export lands, but the category/blog work can proceed now.
